
    <ul class="gallery-list">
        <li class="gallery-item">
            <div class="gallery-image_wrapper"><img src=""alt"">{{image}}</div>
            <p class="gallery-info_name">{{name}}</p>
            <p class="gallery-info_genre">{{#each genre}} | {{year}}<span class="gallery-info_rating">rating</span></p>{{/each}}
        </li>
    </ul>
</div>
